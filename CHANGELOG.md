# Changelog

## Unreleased

- Added a reusable Docker production baseline for web, worker, and PostgreSQL.
- Added `.env.production.example` for production environment setup.
- Documented the live public deployment at `https://dream.vectorcontrol.tech`.
- Added deployment and release runbooks.
- Added `typecheck` and `prisma:deploy` npm scripts; `lint` now runs the
  available TypeScript check instead of the deprecated Next lint command.

## 0.1.0

- Initial DreamSpirit application release.
